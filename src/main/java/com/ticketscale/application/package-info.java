/**
 * Camada de Aplicação.
 * Contém os casos de uso (use cases) e as portas (interfaces) que
 * orquestram o fluxo entre as camadas de domínio e infraestrutura.
 */
package com.ticketscale.application;
